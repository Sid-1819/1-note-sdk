/**
 * Default production API origin when neither `createSecureShareClient({ baseUrl })`
 * nor `process.env.ONE_NOTE_API_URL` (Node) is set. Replace with your deployed API URL.
 */
export const DEFAULT_ONE_NOTE_API_ORIGIN = "https://secureshare-95o6.onrender.com";
